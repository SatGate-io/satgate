# =============================================================================
# SatGate Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# =============================================================================

# ---------------------------------------------------------------------------
# Lightning Node Connect (LNC) Credentials
# ---------------------------------------------------------------------------
# Get these from Terminal Web (https://terminal.lightning.engineering)
# 1. Go to Terminal Web > Lightning Node Connect
# 2. Create a new session or use existing
# 3. Copy the pairing phrase (10 words)

LNC_PASSPHRASE="your ten word pairing phrase goes here"
LNC_NETWORK=mainnet
LNC_MAILBOX=mailbox.terminal.lightning.today:443

# ---------------------------------------------------------------------------
# Port Configuration
# ---------------------------------------------------------------------------
# Aperture (L402 Gateway) - This is your public API endpoint
APERTURE_PORT=8081

# Docs & Playground
DOCS_PORT=8080

# Backend (internal only, not exposed publicly)
# BACKEND_PORT=8083

# ---------------------------------------------------------------------------
# Optional: Production Settings
# ---------------------------------------------------------------------------
# NODE_ENV=production
