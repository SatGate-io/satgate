## SatGate™ Landing Site (`satgate.io`)

This is the SatGate marketing site + playground UI (Next.js).

## Getting Started

Install deps and run the dev server:

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

### Key routes

- `/` marketing homepage
- `/playground` live demo UI
- `/security`, `/terms`, `/privacy`

## Deploy on Vercel

This app is intended to be deployed on Vercel.
